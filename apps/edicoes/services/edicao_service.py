"""Serviço placeholder do domínio de edições."""


class EdicaoService:
    """Agrupa regras futuras do domínio de edições."""

    def listar(self) -> list[dict]:
        """Lista registros quando a regra real existir."""
        raise NotImplementedError("Servico ainda nao esta disponivel.")
